using UnityEngine;

public enum SkillAreaType
{
    Cone,
    Box,
    Circle,
    Arc,
}

public enum Rarity
{
    Normal,
    Rare,
    Epic,
    Legendary
}

public enum WeaponType
{
    Katana,
    Universal,
    
}